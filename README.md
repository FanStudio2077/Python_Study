# Python 学习笔记

学 Python 一个月，现在掌握了：变量、流程分支、循环、列表、JSON、GET 请求。

> 加油，只要今天比昨天好，这就是希望。

## 目录结构

```
Python_Study/
├── 01_基础语法/           # 变量、输入输出、运算符、if 判断
├── 02_流程控制/           # while / for 循环、break / continue
├── 03_列表与算法/         # 列表操作与排序算法
├── 04_JSON与文件操作/     # JSON 读写、文件读写、os 模块
├── 05_网络请求与综合项目/ # requests 请求接口 + 综合小项目
├── 06_综合练习/           # 综合习题
└── 其他/                  # 非 Python 文件（C++ 练习）
```

## 第 1 章　基础语法

| 文件 | 知识点 | 状态 |
| --- | --- | --- |
| `01_成绩等级判断.py` | `if / elif / else` 多分支 | ✅ |
| `02_汇率换算.py` | 输入、类型转换、`if` 嵌套、`or` | ⚠️ 美元汇率写成了人民币汇率 |
| `03_回文数判断.py` | 取各位数字、`%` 与 `int()` | ⚠️ 只支持 5 位数 |
| `04_简易计算器.py` | 多分支 + 除零判断 | ✅ |
| `05_密码验证_未完成.py` | `for` 循环 + 计数 | ❌ 待完成 |

## 第 2 章　流程控制

| 文件 | 知识点 | 状态 |
| --- | --- | --- |
| `01_while累加求平均.py` | `while` + `break` 累计求和求平均 | ✅ |
| `02_continue用法.py` | `continue` 跳过本次循环 | ✅ |
| `03_猜数字游戏.py` | `random` 随机数 + 循环 + 分支 | ✅ |
| `04_九九乘法表_未完成.py` | 嵌套 `for` 起步 | ❌ 语法错误，待完成 |
| `05_九九乘法表.py` | 嵌套 `for` 打印乘法表 | ⚠️ `end=""` 缺分隔符，从 0 开始 |

## 第 3 章　列表与算法

| 文件 | 知识点 | 状态 |
| --- | --- | --- |
| `01_列表基础.py` | 列表创建、`append`、索引 | ✅ |
| `02_冒泡排序.py` | 双重 `while` + 元素交换 | ✅ |

## 第 4 章　JSON 与文件操作

| 文件 | 知识点 | 状态 |
| --- | --- | --- |
| `01_JSON读写练习.py` | `json.load / json.dump`、读写合并、去重 | ✅ |
| `data.json` | 练习用的数据文件（编号 → 积分 / 日期） | - |

> 注意：这几个脚本用的是相对路径 `data.json`，运行时请先把终端 `cd` 到脚本所在目录，
> 否则会在别的地方新建一个空文件。

## 第 5 章　网络请求与综合项目

| 文件 | 知识点 | 状态 |
| --- | --- | --- |
| `01_天气查询.py` | `requests.get` + JSON 解析 | ✅ |
| `02_聊天机器人.py` | 按关键词分发：天气 / 一言 / 签到，并读写 JSON | ✅ |

## 第 6 章　综合练习

| 文件 | 知识点 | 状态 |
| --- | --- | --- |
| `01_自动售卖机.py` | 变量、`while`、`if` 综合应用 | ❌ 只写了开头，待完成 |

## 其他

- `其他/sss.cpp`、`其他/sss.exe`：C++ 练习，与 Python 无关。

## 下一步可以练什么

1. 把第 1、2、6 章标注 ⚠️ / ❌ 的练习补完。
2. 学会用 `def` 把重复代码封装成函数（现在很多逻辑是复制粘贴的）。
3. 学会用 `try / except` 处理输入错误，比如用户输入了字母而不是数字。
4. 了解 `class` 和模块化，把聊天机器人拆成多个文件。
